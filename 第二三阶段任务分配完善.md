# 第二、三阶段任务分配完善补充文档

**文档版本**: v1.7
**创建日期**: 2026-03-13
**说明**: 本文档按照第一阶段的标准细化了第二、三阶段的任务分配

---

# 阶段二：核心功能开发与前后端集成（第4-6周）

## 📋 智能体A任务 - Prompt模板引擎与配置系统完善

### 工作目录
```
C:\Users\yangy\myfile\python\ABM\v0.4.0\agent-a-worktree
```

**说明**：智能体A在独立的工作树中工作，与其他智能体完全隔离。

### Git分支
```bash
# 切换到已有分支或创建新分支
git checkout feature/framework
# 或
git checkout -b feature/framework-phase2
```

### 详细任务清单

#### 任务2.1：完善Prompt模板引擎

**文件位置**: `core/prompt_engine.py`

**接口要求（必须严格遵循）**:
```python
"""
Prompt模板引擎 - 对应技术方案3.2.3节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from enum import Enum
from typing import Dict, Any, List, Optional
from pydantic import BaseModel
import os
import re
from datetime import datetime

class PromptTemplate(Enum):
    """Prompt模板类型枚举"""
    LEADER_DECISION = "leader_decision"
    STATE_FOLLOWUP = "state_followup"
    ALLIANCE_INVITATION = "alliance_invitation"
    ALLIANCE_RESPONSE = "alliance_response"
    GLOBAL_EVENT_RESPONSE = "global_event_response"
    FINAL_JUDGMENT = "final_judgment"
    SITUATION_ASSESSMENT = "situation_assessment"
    POWER_BALANCE_EVALUATION = "power_balance_evaluation"
    INFLUENCE_ANALYSIS = "influence_analysis"
    RULE_VALIDATION = "rule_validation"
    CONSTRAINT_CHECK = "constraint_check"

class PromptBuilder:
    """Prompt构建流水线"""
    def __init__(self, template: PromptTemplate, context: Dict[str, Any]):
        """
        Args:
            template: 使用的模板类型
            context: 初始上下文变量
        """
        pass

    def add_variable(self, key: str, value: Any) -> "PromptBuilder":
        """
        添加单个变量

        Args:
            key: 变量名
            value: 变量值

        Returns:
            self，支持链式调用
        """
        pass

    def add_variables(self, variables: Dict[str, Any]) -> "PromptBuilder":
        """
        添加多个变量

        Args:
            variables: 变量字典

        Returns:
            self，支持链式调用
        """
        pass

    def add_system_message(self, content: str) -> "PromptBuilder":
        """
        添加系统消息

        Args:
            content: 系统消息内容

        Returns:
            self，支持链式调用
        """
        pass

    def add_memory_context(self, memory: List[Dict]) -> "PromptBuilder":
        """
        添加记忆上下文

        Args:
            memory: 记忆列表

        Returns:
            self，支持链式调用
        """
        pass

    def build(self) -> str:
        """
        构建最终Prompt

        Returns:
            完整的Prompt字符串
        """
        pass

class PromptTemplateEngine:
    """Prompt模板引擎"""
    def __init__(self, template_dir: str = "config/prompts/"):
        """
        Args:
            template_dir: 模板文件目录
        """
        pass

    def load_template(self, template_type: PromptTemplate) -> str:
        """
        加载模板文件

        Args:
            template_type: 模板类型

        Returns:
            模板内容字符串
        """
        pass

    def render_template(self, template_type: PromptTemplate, context: Dict[str, Any]) -> str:
        """
        渲染模板

        Args:
            template_type: 模板类型
            context: 渲染上下文

        Returns:
            渲染后的字符串
        """
        pass

    def calculate_token_count(self, text: str) -> int:
        """
        计算Token数量（基于DeepSeek-V3.2的tokenizer）

        Args:
            text: 待计算的文本

        Returns:
            Token数量
        """
        pass
```

**实现内容**:

1. **增强模板变量系统**
   - 实现动态变量注入
   - 支持嵌套变量解析（如 `{agent.power.tier}`）
   - 添加变量类型验证（字符串、数字、数组、对象）
   - 实现变量默认值机制

2. **实现多场景Prompt模板**
   - 严格按照上述PromptTemplate枚举实现
   - 每个模板必须包含完整的场景描述
   - 支持模板版本管理

3. **实现模板缓存机制**
   - 避免重复读取模板文件
   - 支持模板热重载（开发环境）
   - 添加模板版本管理
   - 实现缓存失效机制

4. **实现Prompt构建流水线**
   - 严格按照PromptBuilder类接口实现
   - 支持链式调用
   - 实现内存上下文的智能截断
   - 控制最终Prompt长度在4096 token以内

5. **实现Prompt长度优化**
   - 智能截断过长的记忆内容
   - 优先保留最近和重要的记忆
   - 计算Token使用量（基于DeepSeek-V3.2的tokenizer）

**关键验证点**：
- [ ] PromptTemplate枚举包含所有11种模板类型
- [ ] PromptBuilder支持链式调用
- [ ] 模板渲染支持嵌套变量解析
- [ ] Token计数功能准确
- [ ] 长文本截断逻辑正确

---

#### 任务2.2：完善配置系统

**文件位置**: `config/settings.py`

**接口要求（必须严格遵循）**:
```python
"""
配置系统 - 对应技术方案4.1节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from enum import Enum
from typing import Any, Dict, Optional, List
from pydantic import BaseModel, Field
import os
import yaml
import json
from pathlib import Path
from datetime import datetime

class ConfigSource(Enum):
    FILE = "file"
    ENV_VAR = "env_var"
    DEFAULT = "default"

class ConfigValue(BaseModel):
    value: Any
    source: ConfigSource

class ValidationResult(BaseModel):
    is_valid: bool
    errors: List[str] = Field(default_factory=list)
    warnings: List[str] = Field(default_factory=list)

def load_config_with_env_override(config_path: str) -> "SimulationConfig":
    """
    加载配置并支持环境变量覆盖

    配置优先级：环境变量 > 配置文件 > 默认值

    Args:
        config_path: 配置文件路径

    Returns:
        SimulationConfig配置对象
    """
    pass

def validate_config(config: "SimulationConfig") -> ValidationResult:
    """
    验证配置的有效性

    Args:
        config: 配置对象

    Returns:
        验证结果
    """
    pass

def watch_config_file(config_path: str, callback: callable):
    """
    监听配置文件变化

    Args:
        config_path: 配置文件路径
        callback: 配置变更回调函数
    """
    pass
```

**实现内容**:

1. **添加环境变量支持**
   - 严格按照上述接口实现
   - 环境变量前缀：`MORAL_REALISM_`
   - 支持YAML/JSON配置文件
   - 配置优先级：环境变量 > 配置文件 > 默认值

2. **实现配置验证**
   - 实现完整的配置验证逻辑
   - 验证LLM API密钥格式
   - 验证路径存在性
   - 验证数值范围
   - 返回详细的错误和警告信息

3. **添加配置热重载**
   - 监听配置文件变化（使用watchdog库）
   - 自动应用新配置（部分可热重载项）
   - 配置变更日志记录
   - 支持热重载白名单配置

4. **实现配置版本控制**
   - 记录配置历史（使用SQLite存储）
   - 支持配置回滚
   - 配置差异对比（JSON diff）
   - 配置快照功能

**关键验证点**：
- [ ] 环境变量覆盖机制正常工作
- [ ] 配置验证返回正确的错误信息
- [ ] 文件监听和热重载正常
- [ ] 配置历史记录和回滚功能正常

---

#### 任务2.3：完善事件配置

**文件位置**: `config/event_config.py`

**接口要求（必须严格遵循）**:
```python
"""
事件配置系统 - 对应技术方案4.3节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from enum import Enum
from typing import List, Dict, Tuple, Any
from pydantic import BaseModel, Field

class EventType(Enum):
    NATURAL_DISASTER = "natural_disaster"
    ECONOMIC_CRISIS = "economic_crisis"
    TECHNOLOGICAL_BREAKTHROUGH = "technological_breakthrough"
    DIPLOMATIC_INCIDENT = "diplomatic_incident"
    CUSTOM_EVENT = "custom_event"
    ELECTION_CYCLE = "election_cycle"
    LEADERSHIP_CHANGE = "leadership_change"

class EventProbabilityModel(BaseModel):
    base_probability: float = Field(..., ge=0, le=1)
    seasonal_modifier: Dict[str, float] = Field(default_factory=dict)
    tier_modifier: Dict[str, float] = Field(default_factory=dict)
    cooldown_periods: Dict[EventType, int] = Field(default_factory=dict)

class EventImpact(BaseModel):
    affected_agents: List[str] = Field(default_factory=list)
    power_impact: Dict[str, float] = Field(default_factory=dict)
    relation_impact: Dict[Tuple[str, str], float] = Field(default_factory=dict)
    global_metrics_impact: Dict[str, float] = Field(default_factory=dict)

class EventConfig(BaseModel):
    random_event_probability: float = 0.1
    enable_user_events: bool = True
    periodic_events: Dict[str, Any] = Field(default_factory=dict)
    random_events: List[Dict[str, Any]] = Field(default_factory=list)
    event_probability_model: EventProbabilityModel = Field(default_factory=EventProbabilityModel)
```

**实现内容**:

1. **扩展事件类型**
   - 严格按照EventType枚举实现
   - 每个事件类型必须有完整的中文描述
   - 支持事件类型扩展

2. **实现事件概率模型**
   - 基础概率配置（0-1范围）
   - 季节调整因子（如冬季自然灾害概率更高）
   - 实力层级调整（大国受经济危机影响不同）
   - 冷却期机制（事件触发后一定轮次内不再触发）

3. **实现事件影响评估**
   - 受影响智能体列表
   - 实力影响（正/负）
   - 关系影响（联盟关系变化）
   - 全局指标影响（如规范有效性、冲突水平）

**关键验证点**：
- [ ] EventType枚举包含所有7种事件类型
- [ ] EventProbabilityModel字段验证正确
- [ ] 事件影响计算逻辑合理
- [ ] 概率计算考虑了所有调整因子

---

#### 任务2.4：实现Prompt模板文件

**创建目录**: `config/prompts/`

**模板文件列表**:
1. `config/prompts/leader_decision.txt` - 领导人决策模板
2. `config/prompts/state_followup.txt` - 状态跟进模板
3. `config/prompts/alliance_invitation.txt` - 联盟邀请模板
4. `config/prompts/alliance_response.txt` - 联盟响应模板
5. `config/prompts/global_event_response.txt` - 全球事件响应模板
6. `config/prompts/final_judgment.txt` - 最终判断模板
7. `config/prompts/situation_assessment.txt` - 局势评估模板
8. `config/prompts/power_balance_evaluation.txt` - 实力平衡评估模板
9. `config/prompts/influence_analysis.txt` - 影响力分析模板
10. `config/prompts/rule_validation.txt` - 规则验证模板
11. `config/prompts/constraint_check.txt` - 约束检查模板

**模板格式要求**:
每个模板必须包含以下部分：
```markdown
# 系统提示词
[系统角色定位]

# 当前局势
{current_situation}

# 历史记忆
{recent_memory}

# 可用行动
{available_actions}

# 约束条件
{constraints}

请以JSON格式输出你的决策，包含：
- action_type: 行动类型
- target: 目标对象（如适用）
- parameters: 行动参数
- reasoning: 决策理由（详细说明）
```

### 阶段二Git提交流程

```bash
# 1. 查看当前状态
git status

# 2. 添加修改的文件
git add core/prompt_engine.py
git add config/settings.py
git add config/event_config.py
git add config/prompts/

# 3. 提交更改
git commit -m "feat(backend): 完善Prompt模板引擎和配置系统

- 实现PromptBuilder流水线
- 支持模板缓存和热重载
- 添加环境变量配置支持
- 扩展事件配置系统
- 创建完整Prompt模板文件"

# 4. 推送到远程仓库
git push origin feature/framework
```

### 接口对齐检查清单

**验收前自我检查**:
- [ ] PromptTemplate枚举包含所有11种模板类型
- [ ] PromptBuilder支持链式调用
- [ ] 模板渲染支持嵌套变量解析
- [ ] Token计数功能准确
- [ ] ConfigSource枚举包含FILE/ENV_VAR/DEFAULT
- [ ] 环境变量前缀使用MORAL_REALISM_
- [ ] 配置热重载功能正常
- [ ] EventType枚举包含所有事件类型
- [ ] 所有Prompts模板文件存在
- [ ] 模板格式符合要求

---

## 📋 智能体B任务 - 引擎性能优化与环境仿真完善

### 工作目录
```
C:\Users\yangy\myfile\python\ABM\v0.4.0\agent-b-worktree
```

**说明**：智能体B在独立的工作树中工作，与其他智能体完全隔离。

### Git分支
```bash
git checkout feature/engines
# 或
git checkout -b feature/engines-phase2
```

### 详细任务清单

#### 任务2.1：优化LLM引擎性能

**文件位置**: `core/llm_engine.py`

**接口要求（必须严格遵循）**:
```python
"""
LLM引擎性能优化 - 对应技术方案3.2.1节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from typing import Dict, Any, List, Optional, Callable
from pydantic import BaseModel
import asyncio
import time
import hashlib
import json
from collections import OrderedDict
from datetime import datetime

class LLMCache(BaseModel):
    """LLM请求缓存"""
    max_size: int = 1000
    ttl: int = 3600  # 缓存时间（秒）

    def __init__(self, max_size: int = 1000, ttl: int = 3600):
        self.max_size = max_size
        self.ttl = ttl
        self._cache = OrderedDict()
        self._timestamps = {}
        self._hits = 0
        self._misses = 0

    def get(self, key: str) -> Optional[Dict]:
        """
        获取缓存结果

        Args:
            key: 缓存键

        Returns:
            缓存的值，如果不存在则返回None
        """
        pass

    def set(self, key: str, value: Dict, ttl: Optional[int] = None):
        """
        设置缓存

        Args:
            key: 缓存键
            value: 缓存值
            ttl: 过期时间（秒），如果None则使用默认值
        """
        pass

    def clear(self):
        """清除所有缓存"""
        pass

    def get_stats(self) -> Dict:
        """
        获取缓存统计信息

        Returns:
            包含命中率、大小等统计信息的字典
        """
        pass

class RetryConfig(BaseModel):
    """重试配置"""
    max_retries: int = 3
    backoff_base: float = 1.0
    backoff_factor: float = 2.0
    retryable_errors: List[int] = [429, 500, 502, 503, 504]

class RequestLimiter:
    """并发请求限制器"""
    def __init__(self, max_concurrent: int = 10, rate_limit: int = 60):
        """
        Args:
            max_concurrent: 最大并发数
            rate_limit: 每分钟请求限制
        """
        self.max_concurrent = max_concurrent
        self.rate_limit = rate_limit
        self._semaphore = asyncio.Semaphore(max_concurrent)
        self._request_times = []
        self._lock = asyncio.Lock()

    async def acquire(self):
        """
        获取请求许可

        当同时满足以下条件时允许请求：
        1. 并发数未超限
        2. 速率限制未超限
        """
        pass

    def release(self):
        """释放请求许可"""
        pass

class LLMPerformanceMonitor:
    """LLM性能监控器"""
    def __init__(self):
        self._requests = []
        self._lock = asyncio.Lock()

    def record_request(self, duration: float, success: bool, tokens: int):
        """
        记录请求信息

        Args:
            duration: 请求耗时（秒）
            success: 是否成功
            tokens: 使用的token数量
        """
        pass

    def get_metrics(self) -> Dict[str, Any]:
        """
        获取性能指标

        Returns:
            包含平均延迟、成功率、token使用量等的指标
        """
        pass

    def get_average_latency(self) -> float:
        """
        获取平均延迟

        Returns:
            平均延迟（秒）
        """
        pass

    def get_success_rate(self) -> float:
        """
        获取成功率

        Returns:
            成功率（0-1）
        """
        pass
```

**实现内容**:

1. **实现请求缓存**
   - 键值对：使用prompt + functions的哈希值作为key
   - 过期机制：支持TTL（默认1小时）
   - 缓存淘汰：LRU策略
   - 缓存统计：命中率、大小、过期数量

2. **实现请求重试机制**
   - 指数退避策略
   - 可重试错误码配置
   - 最大重试次数
   - 重试间隔计算

3. **实现并发请求管理**
   - 令牌桶算法实现
   - 并发数限制
   - 速率限制（每分钟请求数）
   - 请求排队机制

4. **添加性能监控**
   - 每个请求的延迟统计
   - 成功率计算
   - Token使用量统计
   - 请求分布分析

**关键验证点**：
- [ ] LLMCache的get/set方法正常工作
- [ ] 重试机制在429错误时正确重试
- [ ] 并发请求限制在10个以内
- [ ] 性能监控指标准确

---

#### 任务2.2：完善环境仿真引擎

**文件位置**: `core/environment.py`

**接口要求（必须严格遵循）**:
```python
"""
环境仿真引擎 - 对应技术方案3.2.2节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from enum import Enum
from typing import List, Dict, Any, Optional
from pydantic import BaseModel
from datetime import datetime

class OrderType(Enum):
    """国际秩序类型"""
    HEGEMONIC = "hegemonic"
    BALANCE_OF_POWER = "balance_of_power"
    RULE_BASED = "rule_based"
    DISORDER = "disorder"
    MIXED = "mixed"

class ConflictStatus(Enum):
    """冲突状态"""
    ONGOING = "ongoing"
    CEASEFIRE = "ceasefire"
    RESOLVED = "resolved"
    ESCALATED = "escalated"

class ConflictResolution(Enum):
    """冲突解决方式"""
    DIPLOMATIC = "diplomatic"
    MILITARY = "military"
    MEDIATION = "mediation"
    STATUS_QUO = "status_quo"

class EnvironmentState(BaseModel):
    """环境状态"""
    round: int
    global_order: OrderType
    power_distribution: Dict[str, float]
    alliance_structure: Dict[str, List[str]]
    pending_events: List[Dict]
    active_conflicts: List[Dict]
    timestamp: datetime

class EventTrigger:
    """事件触发系统"""
    def __init__(self, config: Dict):
        """
        Args:
            config: 事件配置字典
        """
        self.config = config
        self._event_cooldowns = {}

    def check_random_events(self, state: EnvironmentState) -> List[Dict]:
        """
        检查随机事件触发

        Args:
            state: 当前环境状态

        Returns:
            触发的事件列表
        """
        pass

    def trigger_user_event(self, event: Dict) -> Dict:
        """
        触发用户自定义事件

        Args:
            event: 用户事件

        Returns:
            事件结果
        """
        pass

    def trigger_periodic_events(self, state: EnvironmentState) -> List[Dict]:
        """
        触发周期事件

        Args:
            state: 当前环境状态

        Returns:
            触发的周期事件列表
        """
        pass

class ConflictManager:
    """冲突管理"""
    def __init__(self):
        self._conflicts = {}
        self._next_conflict_id = 1

    def create_conflict(self, participants: List[str], cause: str) -> Dict:
        """
        创建冲突

        Args:
            participants: 参与方列表
            cause: 冲突原因

        Returns:
            创建的冲突
        """
        pass

    def update_conflict_status(self, conflict_id: str, status: ConflictStatus):
        """
        更新冲突状态

        Args:
            conflict_id: 冲突ID
            status: 新状态
        """
        pass

    def resolve_conflict(self, conflict_id: str, resolution: ConflictResolution):
        """
        解决冲突

        Args:
            conflict_id: 冲突ID
            resolution: 解决方式
        """
        pass

    def get_active_conflicts(self) -> List[Dict]:
        """
        获取活跃冲突

        Returns:
            活跃冲突列表
        """
        pass

class EnvironmentalEffects:
    """环境效应计算"""
    def calculate_event_impact(self, event: Dict, agents: List[Any]) -> Dict:
        """
        计算事件影响

        Args:
            event: 事件
            agents: 受影响的智能体列表

        Returns:
            事件影响评估
        """
        pass

    def calculate_conflict_spillover(self, conflict: Dict) -> Dict[str, float]:
        """
        计算冲突溢出效应

        Args:
            conflict: 冲突

        Returns:
            溢出效应字典 {agent_id: impact}
        """
        pass

    def calculate_alliance_benefits(self, alliance: Dict) -> Dict[str, float]:
        """
        计算联盟收益

        Args:
            alliance: 联盟

        Returns:
            收益字典 {agent_id: benefit}
        """
        pass
```

**实现内容**:

1. **实现环境状态管理**
   - 完整记录当前轮次的所有环境信息
   - 支持状态序列化和反序列化
   - 状态对比和变化检测

2. **实现事件触发系统**
   - 随机事件触发（基于概率配置）
   - 用户事件触发（手动触发）
   - 周期事件触发（如领导换届）
   - 事件优先级排序

3. **实现冲突管理**
   - 冲突创建和升级
   - 冲突状态跟踪
   - 冲突解决机制
   - 冲突影响传播

4. **实现环境效应计算**
   - 事件对智能体的直接影响
   - 冲突的间接溢出效应
   - 联盟的长期收益计算
   - 全局指标的变化

**关键验证点**：
- [ ] EnvironmentState包含所有环境信息
- [ ] 事件触发概率计算准确
- [ ] 冲突管理流程完整
- [ ] 环境效应计算逻辑合理

---

### 阶段二Git提交流程

```bash
git add core/llm_engine.py
git add core/environment.py
git add core/validator.py
git add core/storage.py

git commit -m "feat(backend): 优化引擎性能和完善环境仿真

- 实现LLM请求缓存和重试机制
- 添加性能监控和流式响应
- 完善环境仿真引擎（事件、冲突、效应）
- 实现复杂规则验证和规则链
- 增强数据存储引擎（优化、归档、备份）"

git push origin feature/engines
```

---

## 📋 智能体C任务 - 智能体决策逻辑完善与互动规则实现

### 工作目录
```
C:\Users\yangy\myfile\python\ABM\v0.4.0\agent-c-worktree
```

**说明**：智能体C在独立的工作树中工作，与其他智能体完全隔离。

### Git分支
```bash
git checkout feature/entities
# 或
git checkout -b feature/entities-phase2
```

### 详细任务清单

#### 任务2.1：完善智能体决策逻辑

**文件位置**: `entities/base_agent.py`

**接口要求（必须严格遵循）**:
```python
"""
智能体决策逻辑完善 - 对应技术方案3.3.1节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from enum import Enum
from typing import List, Dict, Any, Optional
from pydantic import BaseModel
import hashlib
import json
from datetime import datetime

class DecisionPriority(Enum):
    """决策优先级"""
    EMERGENCY = 5
    HIGH = 4
    MEDIUM = 3
    LOW = 2
    ROUTINE = 1

class DecisionCache:
    """决策缓存"""
    def __init__(self, max_size: int = 100, ttl: int = 3600):
        self._cache = {}
        self._timestamps = {}
        self.max_size = max_size
        self.ttl = ttl

    def cache_decision(self, context_hash: str, decision: Dict):
        """
        缓存决策

        Args:
            context_hash: 上下文哈希
            decision: 决策结果
        """
        pass

    def get_cached_decision(self, context_hash: str) -> Optional[Dict]:
        """
        获取缓存的决策

        Args:
            context_hash: 上下文哈希

        Returns:
            缓存的决策，如果不存在则返回None
        """
        pass

    def invalidate(self, agent_id: str):
        """
        使特定智能体的缓存失效

        Args:
            agent_id: 智能体ID
        """
        pass

class ConsistencyReport(BaseModel):
    """一致性报告"""
    is_consistent: bool
    issues: List[str] = []
    confidence_score: float = 0.0

class AgentLearning:
    """智能体学习机制"""
    def __init__(self, agent_id: str):
        self.agent_id = agent_id
        self._outcomes = []
        self._preferences = {}

    def record_outcome(self, decision: Dict, outcome: Dict):
        """
        记录决策结果

        Args:
            decision: 决策
            outcome: 结果
        """
        pass

    def update_preferences(self, feedback: Dict):
        """
        更新偏好

        Args:
            feedback: 反馈信息
        """
        pass

    def get_learned_preferences(self) -> Dict[str, float]:
        """
        获取学习到的偏好

        Returns:
            偏好字典
        """
        pass

class BaseAgent:
    """智能体基类 - 决策逻辑增强"""
    def evaluate_decision_priority(self, situation: Dict) -> DecisionPriority:
        """
        评估决策优先级

        Args:
            situation: 当前局势

        Returns:
            决策优先级
        """
        pass

    def check_decision_consistency(
        self,
        new_decision: Dict,
        recent_decisions: List[Dict]
    ) -> ConsistencyReport:
        """
        检查决策一致性

        Args:
            new_decision: 新决策
            recent_decisions: 近期决策列表

        Returns:
            一致性报告
        """
        pass
```

**实现内容**:

1. **实现决策缓存机制**
   - 上下文哈希计算
   - LRU缓存淘汰
   - 缓存失效策略

2. **实现决策优先级系统**
   - 紧急情况判断
   - 优先级评估规则
   - 高优先级决策处理

3. **实现决策一致性检查**
   - 与历史决策对比
   - 一致性评分
   - 问题报告生成

4. **实现学习机制**
   - 结果记录
   - 偏好更新
   - 学习成果应用

**关键验证点**：
- [ ] 决策缓存命中和失效正常
- [ ] 优先级评估逻辑合理
- [ ] 一致性检查能发现矛盾决策
- [ ] 学习机制正确记录结果

---

#### 任务2.2：完善大国智能体

**文件位置**: `entities/state_agent.py`

**接口要求（必须严格遵循）**:
```python
"""
大国智能体完善 - 对应技术方案3.3.2节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from typing import List, Dict, Any, Optional
from pydantic import BaseModel

class GlobalAssessment(BaseModel):
    """全球局势评估"""
    power_balance: Dict[str, float]
    alliance_situation: Dict
    threat_level: float
    opportunity_level: float
    recommended_strategy: str

class LeadershipDecision(BaseModel):
    """领导决策"""
    action_type: str
    priority: int
    target_agents: List[str]
    parameters: Dict
    reasoning: str

class RegionalAssessment(BaseModel):
    """区域局势评估"""
    region: str
    power_structure: Dict
    stability_level: float
    local_interests: List[str]

class RegionalPolicy(BaseModel):
    """区域政策"""
    region: str
    policy_type: str
    actions: List[Dict]
    expected_outcome: str

class AllianceProposal(BaseModel):
    """联盟提议"""
    proposal_id: str
    proposer: str
    target: str
    alliance_type: str
    terms: Dict
    proposal_time: str

class AllianceResponse(BaseModel):
    """联盟响应"""
    proposal_id: str
    response_type: str  # accept, reject, counter_proposal
    reasoning: str
    counter_terms: Optional[Dict] = None

class GlobalLeadershipStrategy:
    """全球领导决策模块"""
    def __init__(self, agent: Any):
        """
        Args:
            agent: 大国智能体实例
        """
        self.agent = agent

    def assess_global_situation(self) -> GlobalAssessment:
        """
        评估全球局势

        Returns:
            全球局势评估
        """
        pass

    def formulate_leadership_strategy(self) -> LeadershipDecision:
        """
        制定领导策略

        Returns:
            领导决策
        """
        pass

    def evaluate_leadership_outcome(self, outcome: Dict):
        """
        评估领导结果

        Args:
            outcome: 执行结果
        """
        pass

class RegionalManagementStrategy:
    """区域管理决策模块"""
    def __init__(self, agent: Any):
        self.agent = agent

    def assess_regional_situation(self, region: str) -> RegionalAssessment:
        """
        评估区域局势

        Args:
            region: 区域名称

        Returns:
            区域局势评估
        """
        pass

    def formulate_regional_policy(self, region: str) -> RegionalPolicy:
        """
        制定区域政策

        Args:
            region: 区域名称

        Returns:
            区域政策
        """
        pass

    def manage_regional_relations(self, region: str) -> List[Dict]:
        """
        管理区域关系

        Args:
            region: 区域名称

        Returns:
            关系行动列表
        """
        pass

class AllianceManager:
    """联盟管理模块"""
    def __init__(self, agent: Any):
        self.agent = agent
        self._proposals = {}

    def propose_alliance(self, target_id: str) -> AllianceProposal:
        """
        提议建立联盟

        Args:
            target_id: 目标智能体ID

        Returns:
            联盟提议
        """
        pass

    def evaluate_alliance_request(self, proposal: AllianceProposal) -> AllianceResponse:
        """
        评估联盟请求

        Args:
            proposal: 联盟提议

        Returns:
            联盟响应
        """
        pass

    def maintain_alliance(self, alliance_id: str) -> Dict:
        """
        维护联盟

        Args:
            alliance_id: 联盟ID

        Returns:
            维护行动
        """
        pass

    def dissolve_alliance(self, alliance_id: str) -> Dict:
        """
        解散联盟

        Args:
            alliance_id: 联盟ID

        Returns:
            解散结果
        """
        pass

class StateAgent:
    """大国智能体"""
    def __init__(self, agent_config: Dict):
        self.config = agent_config
        self.global_strategy = GlobalLeadershipStrategy(self)
        self.regional_strategy = RegionalManagementStrategy(self)
        self.alliance_manager = AllianceManager(self)
```

**实现内容**:

1. **实现全球领导决策模块**
   - 全球局势评估
   - 领导策略制定
   - 结果评估反馈

2. **实现区域管理决策模块**
   - 区域局势分析
   - 区域政策制定
   - 区域关系管理

3. **实现联盟管理模块**
   - 联盟提议
   - 联盟请求评估
   - 联盟维护
   - 联盟解散

**关键验证点**：
- [ ] 全球局势评估包含所有维度
- [ ] 区域政策能适应不同区域
- [ ] 联盟管理流程完整
- [ ] 决策都有详细理由

---

#### 任务2.3：完善小国智能体

**文件位置**: `entities/small_state_agent.py`

**接口要求（必须严格遵循）**:
```python
"""
小国智能体完善 - 对应技术方案3.3.3节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from typing import List, Dict, Any, Optional
from pydantic import BaseModel

class LeaderCandidate(BaseModel):
    """领导人候选"""
    agent_id: str
    name: str
    power_score: float
    reliability_score: float
    benefit_assessment: Dict

class BenefitAssessment(BaseModel):
    """收益评估"""
    security_benefit: float
    economic_benefit: float
    political_benefit: float
    total_benefit: float
    risks: List[str]

class SurvivalThreat(BaseModel):
    """生存威胁"""
    threat_type: str
    source: str
    severity: float
    urgency: float
    description: str

class SurvivalAction(BaseModel):
    """生存行动"""
    action_type: str
    target: Optional[str]
    parameters: Dict
    reasoning: str

class ProtectionRequest(BaseModel):
    """保护请求"""
    target_protector: str
    request_type: str
    terms: Dict
    reasoning: str

class RelationAdjustment(BaseModel):
    """关系调整"""
    target_agent: str
    adjustment_type: str
    new_relation_level: float
    reasoning: str

class FollowerStrategy:
    """追随选择策略"""
    def __init__(self, agent: Any):
        """
        Args:
            agent: 小国智能体实例
        """
        self.agent = agent

    def identify_potential_leaders(self) -> List[LeaderCandidate]:
        """
        识别潜在领导者

        Returns:
            领导者候选列表
        """
        pass

    def evaluate_leader_benefits(self, leader: Any) -> BenefitAssessment:
        """
        评估领导者收益

        Args:
            leader: 领导者

        Returns:
            收益评估
        """
        pass

    def choose_leader(self) -> Optional[str]:
        """
        选择领导者

        Returns:
            选择的领导者ID，或者None
        """
        pass

    def adjust_follower_relationship(self) -> RelationAdjustment:
        """
        调整追随者关系

        Returns:
            关系调整
        """
        pass

class SurvivalStrategy:
    """生存策略"""
    def __init__(self, agent: Any):
        self.agent = agent

    def assess_survival_threats(self) -> List[SurvivalThreat]:
        """
        评估生存威胁

        Returns:
            威胁列表
        """
        pass

    def formulate_survival_response(self, threat: SurvivalThreat) -> SurvivalAction:
        """
        制定生存响应

        Args:
            threat: 威胁

        Returns:
            生存行动
        """
        pass

    def seek_protection(self) -> ProtectionRequest:
        """
        寻求保护

        Returns:
            保护请求
        """
        pass

class SmallStateAgent:
    """小国智能体"""
    def __init__(self, agent_config: Dict):
        self.config = agent_config
        self.follower_strategy = FollowerStrategy(self)
        self.survival_strategy = SurvivalStrategy(self)
```

**实现内容**:

1. **实现追随选择逻辑**
   - 识别潜在领导者
   - 评估收益和风险
   - 选择最优领导者
   - 调整追随关系

2. **实现生存策略**
   - 评估生存威胁
   - 制定响应策略
   - 寻求保护

**关键验证点**：
- [ ] 领导者选择考虑收益和风险
- [ ] 生存威胁评估全面
- [ ] 追随策略能适应变化
- [ ] 所有决策有详细理由

---

#### 任务2.4：实现完整互动规则

**文件位置**: `entities/interaction_rules.py`

**接口要求（必须严格遵循）**:
```python
"""
互动规则实现 - 对应技术方案3.3.4节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from enum import Enum
from typing import List, Dict, Any, Tuple, Optional
from pydantic import BaseModel

class InteractionType(Enum):
    """互动类型"""
    FORM_ALLIANCE = "form_alliance"
    SIGN_TREATY = "sign_treaty"
    PROVIDE_AID = "provide_aid"
    DIPLOMATIC_SUPPORT = "diplomatic_support"
    DECLARE_WAR = "declare_war"
    IMPOSE_SANCTIONS = "impose_sanctions"
    DIPLOMATIC_PROTEST = "diplomatic_protest"
    SEND_MESSAGE = "send_message"
    HOLD_SUMMIT = "hold_summit"
    PUBLIC_STATEMENT = "public_statement"
    ECONOMIC_PRESSURE = "economic_pressure"
    CULTURAL_INFLUENCE = "cultural_influence"
    MILITARY_POSTURE = "military_posture"

class Interaction(BaseModel):
    """互动"""
    interaction_id: str
    interaction_type: InteractionType
    source_agent: str
    target_agent: Optional[str]
    parameters: Dict
    timestamp: str
    round: int

class ValidationResult(BaseModel):
    """验证结果"""
    is_valid: bool
    errors: List[str] = []
    warnings: List[str] = []

class Constraint(BaseModel):
    """约束"""
    constraint_type: str
    description: str
    severity: float

class RelationChange(BaseModel):
    """关系变化"""
    agent_pair: Tuple[str, str]
    change_amount: float
    reason: str

class PowerChange(BaseModel):
    """实力变化"""
    agent_id: str
    change_amount: float
    reason: str

class GlobalChange(BaseModel):
    """全局变化"""
    metric: str
    change_amount: float
    reason: str

class ThirdPartyEffect(BaseModel):
    """第三方效应"""
    agent_id: str
    effect_type: str
    impact: float
    reason: str

class InteractionPattern(BaseModel):
    """互动模式"""
    agent_id: str
    frequent_actions: List[InteractionType]
    preferred_partners: List[str]
    action_tendencies: Dict[InteractionType, float]

class PredictedInteraction(BaseModel):
    """预测互动"""
    predicted_type: InteractionType
    source_agent: str
    target_agent: Optional[str]
    confidence: float
    reasoning: str

class InteractionRules:
    """互动规则"""
    def __init__(self, config: Dict):
        """
        Args:
            config: 规则配置
        """
        self.config = config

    def validate_interaction(self, interaction: Interaction) -> ValidationResult:
        """
        验证互动

        Args:
            interaction: 互动

        Returns:
            验证结果
        """
        pass

    def get_allowed_interactions(self, agent: Any) -> List[InteractionType]:
        """
        获取允许的互动

        Args:
            agent: 智能体

        Returns:
            允许的互动类型列表
        """
        pass

    def get_forbidden_interactions(self, agent: Any) -> List[InteractionType]:
        """
        获取禁止的互动

        Args:
            agent: 智能体

        Returns:
            禁止的互动类型列表
        """
        pass

    def check_interaction_constraints(self, interaction: Interaction) -> List[Constraint]:
        """
        检查互动约束

        Args:
            interaction: 互动

        Returns:
            约束列表
        """
        pass

class InteractionImpactCalculator:
    """互动影响计算器"""
    def calculate_relation_impact(self, interaction: Interaction) -> RelationChange:
        """
        计算关系影响

        Args:
            interaction: 互动

        Returns:
            关系变化
        """
        pass

    def calculate_power_impact(self, interaction: Interaction) -> PowerChange:
        """
        计算实力影响

        Args:
            interaction: 互动

        Returns:
            实力变化
        """
        pass

    def calculate_global_impact(self, interaction: Interaction) -> GlobalChange:
        """
        计算全局影响

        Args:
            interaction: 互动

        Returns:
            全局变化
        """
        pass

    def calculate_third_party_effects(self, interaction: Interaction) -> List[ThirdPartyEffect]:
        """
        计算第三方效应

        Args:
            interaction: 互动

        Returns:
            第三方效应列表
        """
        pass

class InteractionTracker:
    """互动追踪器"""
    def __init__(self):
        self._history = []

    def record_interaction(self, interaction: Interaction, result: Dict):
        """
        记录互动

        Args:
            interaction: 互动
            result: 结果
        """
        pass

    def get_interaction_history(self, agent_id: str) -> List[Interaction]:
        """
        获取互动历史

        Args:
            agent_id: 智能体ID

        Returns:
            互动列表
        """
        pass

    def get_interaction_patterns(self, agent_id: str) -> InteractionPattern:
        """
        获取互动模式

        Args:
            agent_id: 智能体ID

        Returns:
            互动模式
        """
        pass

    def predict_future_interactions(self, agent: Any) -> List[PredictedInteraction]:
        """
        预测未来互动

        Args:
            agent: 智能体

        Returns:
            预测互动列表
        """
        pass
```

**实现内容**:

1. **定义互动类型**
   - 完整的互动类型枚举
   - 每种类型的描述
   - 使用场景说明

2. **实现互动规则验证**
   - 互动有效性检查
   - 约束条件检查
   - 权限验证

3. **实现互动影响计算**
   - 关系影响计算
   - 实力影响计算
   - 全局影响计算
   - 第三方效应

4. **实现互动历史追踪**
   - 互动记录
   - 模式分析
   - 未来预测

**关键验证点**：
- [ ] 所有互动类型都有定义
- [ ] 验证规则能阻止非法互动
- [ ] 影响计算逻辑合理
- [ ] 历史记录完整准确

---

### 阶段二Git提交流程

```bash
git add entities/base_agent.py
git add entities/state_agent.py
git add entities/small_state_agent.py
git add entities/interaction_rules.py
git add entities/organization_agent.py

git commit -m "feat(entities): 完善智能体决策逻辑和互动规则

- 实现决策缓存、优先级和学习机制
- 完善大国智能体（全球领导、区域管理、联盟）
- 完善小国智能体（追随策略、生存策略）
- 实现完整互动规则和影响计算
- 实现国际组织智能体"

git push origin feature/entities
```

---

## 📋 智能体D任务 - 观测迭代层完善工作流编排

### 工作目录
```
C:\Users\yangy\myfile\python\ABM\v0.4.0\agent-d-worktree
```

**说明**：智能体D在独立的工作树中工作，与其他智能体完全隔离。

### Git分支
```bash
git checkout feature/observation
# 或
git checkout -b feature/observation-phase2
```

### 详细任务清单

#### 任务2.1：完善指标计算模块

**文件位置**: `observation/metrics.py`

**接口要求（必须严格遵循）**:
```python
"""
指标计算模块完善 - 对应技术方案3.4.1节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from typing import List, Dict, Any, Optional
from pydantic import BaseModel
import numpy as np
from collections import defaultdict
from datetime import datetime

class Metric(BaseModel):
    """指标"""
    name: str
    value: float
    category: str
    timestamp: str
    round: int
    metadata: Dict = {}

class MetricsResult(BaseModel):
    """指标结果"""
    simulation_id: str
    round: int
    metrics: Dict[str, List[Metric]]
    timestamp: str

class CalculationContext(BaseModel):
    """计算上下文"""
    simulation_id: str
    round: int
    agents: List[Dict]
    interactions: List[Dict]
    events: List[Dict]
    previous_state: Dict

class MetricCalculator(ABC):
    """指标计算器基类"""
    @abstractmethod
    def get_name(self) -> str:
        pass

    @abstractmethod
    def calculate(self, context: CalculationContext) -> List[Metric]:
        pass

class MetricsPipeline:
    """指标计算流水线"""
    def __init__(self, calculators: List[MetricCalculator]):
        """
        Args:
            calculators: 指标计算器列表
        """
        self.calculators = calculators
        self._cache = {}

    def register_calculator(self, calculator: MetricCalculator):
        """
        注册计算器

        Args:
            calculator: 指标计算器
        """
        pass

    def calculate_all(self, context: CalculationContext) -> MetricsResult:
        """
        计算所有指标

        Args:
            context: 计算上下文

        Returns:
            指标结果
        """
        pass

    def calculate_incremental(self, context: CalculationContext) -> MetricsResult:
        """
        增量计算指标

        Args:
            context: 计算上下文

        Returns:
            指标结果
        """
        pass

class MetricsCache:
    """指标缓存"""
    def __init__(self):
        self._cache = {}
        self._timestamps = {}

    def get_cached_metrics(self, simulation_id: str, round: int) -> Optional[MetricsResult]:
        """
        获取缓存的指标

        Args:
            simulation_id: 仿真ID
            round: 轮次

        Returns:
            缓存的指标，如果不存在则返回None
        """
        pass

    def cache_metrics(self, simulation_id: str, round: int, metrics: MetricsResult):
        """
        缓存指标

        Args:
            simulation_id: 仿真ID
            round: 轮次
            metrics: 指标结果
        """
        pass

    def invalidate_cache(self, simulation_id: str):
        """
        使特定仿真的缓存失效

        Args:
            simulation_id: 仿真ID
        """
        pass

class TrendResult(BaseModel):
    """趋势结果"""
    direction: str  # up, down, stable
    slope: float
    confidence: float
    description: str

class Anomaly(BaseModel):
    """异常"""
    position: int
    value: float
    expected_value: float
    severity: float
    description: str

class TrendAnalyzer:
    """趋势分析器"""
    def analyze_trend(self, metrics: List[float]) -> TrendResult:
        """
        分析趋势

        Args:
            metrics: 指标序列

        Returns:
            趋势结果
        """
        pass

    def detect_anomalies(self, metrics: List[float], method: str = "zscore") -> List[Anomaly]:
        """
        检测异常

        Args:
            metrics: 指标序列
            method: 检测方法

        Returns:
            异常列表
        """
        pass

    def forecast_trend(self, metrics: List[float], periods: int) -> List[float]:
        """
        预测趋势

        Args:
            metrics: 指标序列
            periods: 预测周期数

        Returns:
            预测值列表
        """
        pass
```

**实现内容**:

1. **实现指标计算流水线**
   - 计算器注册和管理
   - 并行计算优化
   - 增量计算支持

2. **实现指标缓存**
   - 结果缓存
   - 缓存失效策略
   - 性能优化

3. **实现指标趋势分析**
   - 趋势方向识别
   - 异常检测
   - 趋势预测

**关键验证点**：
- [ ] 所有指标都能正确计算
- [ ] 缓存机制正常工作
- [ ] 趋势分析准确
- [ ] 异常检测能发现显著偏离

---

#### 任务2.2：完善数据追溯分析

**文件位置**: `observation/analytics.py`

**接口要求（必须严格遵循）**:
```python
"""
数据追溯分析 - 对应技术方案3.4.2节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from typing import List, Dict, Any, Optional
from pydantic import BaseModel
from datetime import datetime

class CausalFactor(BaseModel):
    """因果因素"""
    factor_id: str
    factor_type: str
    description: str
    contribution: float
    evidence: List[str]

class CausalChain(BaseModel):
    """因果链"""
    chain_id: str
    start_event: Dict
    end_event: Dict
    factors: List[CausalFactor]
    total_contribution: float
    visualization_data: Dict

class InfluenceScore(BaseModel):
    """影响力评分"""
    agent_id: str
    total_influence: float
    influence_breakdown: Dict[str, float]
    most_influential_actions: List[Dict]

class ImpactAnalysis(BaseModel):
    """影响分析"""
    event_id: str
    event_type: str
    affected_agents: List[str]
    impact_magnitude: Dict[str, float]
    duration: int
    propagation_path: List[str]

class HeatmapData(BaseModel):
    """热力图数据"""
    matrix: List[List[float]]
    row_labels: List[str]
    col_labels: List[str]
    timestamp: str

class CausalTracer:
    """因果追溯器"""
    def trace_decision_outcome(self, decision_id: str) -> CausalChain:
        """
        追溯决策结果

        Args:
            decision_id: 决策ID

        Returns:
            因果链
        """
        pass

    def find_root_causes(self, outcome_id: str) -> List[CausalFactor]:
        """
        查找根本原因

        Args:
            outcome_id: 结果ID

        Returns:
            因果因素列表
        """
        pass

    def visualize_causal_chain(self, chain: CausalChain) -> Dict:
        """
        可视化因果链

        Args:
            chain: 因果链

        Returns:
            可视化数据
        """
        pass

class InfluenceAnalyzer:
    """影响力分析器"""
    def calculate_agent_influence(self, agent_id: str, simulation_id: str) -> InfluenceScore:
        """
        计算智能体影响力

        Args:
            agent_id: 智能体ID
            simulation_id: 仿真ID

        Returns:
            影响力评分
        """
        pass

    def calculate_event_impact(self, event_id: str) -> ImpactAnalysis:
        """
        计算事件影响

        Args:
            event_id: 事件ID

        Returns:
            影响分析
        """
        pass

    def generate_influence_heatmap(self, simulation_id: str) -> HeatmapData:
        """
        生成影响力热力图

        Args:
            simulation_id: 仿真ID

        Returns:
            热力图数据
        """
        pass

class ComparisonReport(BaseModel):
    """对比报告"""
    simulation_ids: List[str]
    metrics_comparison: Dict[str, Dict[str, float]]
    key_differences: List[Dict]
    summary: str

class ScenarioComparison(BaseModel):
    """场景对比"""
    scenario_name: str
    base_simulation: str
    test_simulation: str
    differences: Dict
    insights: List[str]

class Chart(BaseModel):
    """图表"""
    chart_type: str
    title: str
    data: Dict
    format: str

class ComparisonAnalyzer:
    """对比分析器"""
    def compare_simulations(self, sim_ids: List[str]) -> ComparisonReport:
        """
        对比多次仿真

        Args:
            sim_ids: 仿真ID列表

        Returns:
            对比报告
        """
        pass

    def compare_scenarios(self, scenario_id: str) -> ScenarioComparison:
        """
        对比场景

        Args:
            scenario_id: 场景ID

        Returns:
            场景对比
        """
        pass

    def generate_comparison_charts(self, comparison: ComparisonReport) -> List[Chart]:
        """
        生成对比图表

        Args:
            comparison: 对比报告

        Returns:
            图表列表
        """
        pass
```

**实现内容**:

1. **实现因果追溯**
   - 决策-结果追溯
   - 根因分析
   - 因果链可视化

2. **实现影响力分析**
   - 智能体影响力计算
   - 事件影响评估
   - 热力图生成

3. **实现对比分析**
   - 多仿真对比
   - 场景对比
   - 对比图表

**关键验证点**：
- [ ] 因果追溯能找到正确路径
- [ ] 影响力计算合理
- [ ] 对比分析能发现关键差异
- [ ] 可视化数据格式正确

---

#### 任务2.3：完善工作流编排

**文件位置**: `workflows/multi_round.py`

**接口要求（必须严格遵循）**:
```python
"""
工作流编排完善 - 对应技术方案3.5节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from enum import Enum
from typing import List, Dict, Any, Optional
from pydantic import BaseModel
import asyncio
from datetime import datetime

class WorkflowState(Enum):
    """工作流状态"""
    INITIALIZED = "initialized"
    RUNNING = "running"
    PAUSED = "paused"
    COMPLETED = "completed"
    FAILED = "failed"
    CANCELLED = "cancelled"

class WorkflowEvent(Enum):
    """工作流事件"""
    START = "start"
    PAUSE = "pause"
    RESUME = "resume"
    CANCEL = "cancel"
    COMPLETE = "complete"
    FAIL = "fail"

class WorkflowStateMachine:
    """工作流状态机"""
    def __init__(self, initial_state: WorkflowState):
        """
        Args:
            initial_state: 初始状态
        """
        self._state = initial_state
        self._transition_history = []

    def transition(self, event: WorkflowEvent) -> WorkflowState:
        """
        状态转换

        Args:
            event: 触发事件

        Returns:
            新状态
        """
        pass

    def get_current_state(self) -> WorkflowState:
        """
        获取当前状态

        Returns:
            当前状态
        """
        pass

    def get_allowed_transitions(self) -> List[WorkflowEvent]:
        """
        获取允许的转换

        Returns:
            允许的事件列表
        """
        pass

class Checkpoint(BaseModel):
    """检查点"""
    checkpoint_id: str
    simulation_id: str
    round: int
    state: Dict
    timestamp: str
    metadata: Dict

class WorkflowRecovery:
    """工作流恢复"""
    def __init__(self, checkpoint_dir: str = "data/checkpoints/"):
        self.checkpoint_dir = checkpoint_dir

    def save_checkpoint(self, workflow: Any) -> Checkpoint:
        """
        保存检查点

        Args:
            workflow: 工作流实例

        Returns:
            检查点
        """
        pass

    def load_checkpoint(self, checkpoint_id: str) -> Any:
        """
        加载检查点

        Args:
            checkpoint_id: 检查点ID

        Returns:
            工作流实例
        """
        pass

    def resume_from_checkpoint(self, checkpoint_id: str) -> Dict:
        """
        从检查点恢复

        Args:
            checkpoint_id: 检查点ID

        Returns:
            恢复结果
        """
        pass

class ProgressInfo(BaseModel):
    """进度信息"""
    simulation_id: str
    current_round: int
    total_rounds: int
    percentage: float
    estimated_time_remaining: Optional[float]
    current_stage: str

class ParallelWorkflowExecutor:
    """并行工作流执行器"""
    def __init__(self, max_workers: int = 4):
        """
        Args:
            max_workers: 最大工作线程数
        """
        self.max_workers = max_workers
        self._semaphore = asyncio.Semaphore(max_workers)

    async def execute_parallel(self, workflows: List[Any]) -> List[Dict]:
        """
        并行执行工作流

        Args:
            workflows: 工作流列表

        Returns:
            结果列表
        """
        pass

    def monitor_progress(self) -> ProgressInfo:
        """
        监控进度

        Returns:
            进度信息
        """
        pass

class MultiRoundWorkflow:
    """多轮工作流"""
    def __init__(self, config: Dict):
        self.config = config
        self.state_machine = WorkflowStateMachine(WorkflowState.INITIALIZED)
        self.recovery = WorkflowRecovery()
        self.executor = ParallelWorkflowExecutor()
```

**实现内容**:

1. **实现工作流状态机**
   - 状态定义和转换
   - 状态验证
   - 转换历史记录

2. **实现工作流恢复**
   - 检查点保存
   - 检查点加载
   - 断点续传

3. **实现工作流并行执行**
   - 并行调度
   - 进度监控
   - 错误处理

**关键验证点**：
- [ ] 状态转换符合规则
- [ ] 检查点保存和加载正常
- [ ] 并行执行无竞争
- [ ] 恢复能继续之前进度

---

#### 任务2.4：实现对照实验管理

**文件位置**: `observation/experiments.py`

**接口要求（必须严格遵循）**:
```python
"""
对照实验管理 - 对应技术方案3.4.3节

Git提交用户名: yangyh-2025
Git提交邮箱: yangyuhang2667@163.com
"""
from enum import Enum
from typing import List, Dict, Any, Optional
from pydantic import BaseModel
from datetime import datetime

class ExperimentType(Enum):
    """实验类型"""
    PARAMETER_SWEEP = "parameter_sweep"
    AGENT_CONFIGURATION = "agent_configuration"
    EVENT_SCENARIO = "event_scenario"
    SENSITIVITY_ANALYSIS = "sensitivity_analysis"

class ExperimentDefinition(BaseModel):
    """实验定义"""
    experiment_id: str
    experiment_type: ExperimentType
    name: str
    description: str
    base_config: Dict
    variations: List[Dict]
    parameters: Dict

class Experiment(BaseModel):
    """实验"""
    experiment_id: str
    definition: ExperimentDefinition
    status: str
    created_at: str
    started_at: Optional[str]
    completed_at: Optional[str]
    results: Optional[Dict]

class BatchResult(BaseModel):
    """批量结果"""
    experiment_id: str
    successful: List[str]
    failed: List[str]
    results: Dict[str, Dict]
    summary: str

class AnalysisReport(BaseModel):
    """分析报告"""
    experiment_id: str
    key_findings: List[str]
    statistical_summary: Dict
    visualizations: List[Dict]
    recommendations: List[str]

class ExperimentFramework:
    """实验框架"""
    def __init__(self, config: Dict):
        """
        Args:
            config: 实验配置
        """
        self.config = config
        self._experiments = {}

    def define_experiment(self, definition: ExperimentDefinition) -> Experiment:
        """
        定义实验

        Args:
            definition: 实验定义

        Returns:
            实验
        """
        pass

    def run_experiment(self, experiment: Experiment) -> Dict:
        """
        运行实验

        Args:
            experiment: 实验

        Returns:
            实验结果
        """
        pass

    def run_batch_experiments(self, experiments: List[Experiment]) -> BatchResult:
        """
        批量运行实验

        Args:
            experiments: 实验列表

        Returns:
            批量结果
        """
        pass

class ExperimentAnalyzer:
    """实验分析器"""
    def __init__(self):
        pass

    def analyze_results(self, results: List[Dict]) -> AnalysisReport:
        """
        分析结果

        Args:
            results: 结果列表

        Returns:
            分析报告
        """
        pass

    def generate_report(self, analysis: AnalysisReport) -> str:
        """
        生成报告

        Args:
            analysis: 分析报告

        Returns:
            报告文本
        """
        pass

    def export_results(self, results: List[Dict], format: str) -> str:
        """
        导出结果

        Args:
            results: 结果列表
            format: 格式

        Returns:
            导出文件路径
        """
        pass
```

**实现内容**:

1. **定义实验类型**
   - 参数扫描
   - 智能体配置变化
   - 事件场景
   - 敏感性分析

2. **实现实验框架**
   - 实验定义
   - 实验执行
   - 批量运行

3. **实现实验结果分析**
   - 结果分析
   - 报告生成
   - 结果导出

**关键验证点**：
- [ ] 所有实验类型都支持
- [ ] 批量运行高效稳定
- [ ] 分析报告全面
- [ ] 导出功能可用

---

### 阶段二Git提交流程

```bash
git add observation/metrics.py
git add observation/analytics.py
git add workflows/multi_round.py
git add observation/experiments.py

git commit -m "feat(workflows): 完善观测迭代层和工作流编排

- 实现指标计算流水线和缓存
- 完善数据追溯分析（因果、影响力、对比）
- 实现工作流状态机和恢复机制
- 实现对照实验管理和分析结果"

git push origin feature/observation
```

---

# 阶段三：系统优化与部署（第7-9周）

## 📋 智能体A任务 - 文档完善与部署配置

### 工作目录
```
C:\Users\yangy\myfile\python\ABM\v0.4.0\agent-a-worktree
```

**说明**：智能体A在独立的工作树中工作，与其他智能体完全隔离。

### Git分支
```bash
git checkout feature/framework
# 或
git checkout -b feature/framework-phase3
```

### 详细任务清单

#### 任务3.1：完善项目文档

**创建文件**:

1. **README.md** - 项目主文档
   ```markdown
   # 基于道义现实主义的LLM驱动社会模拟仿真系统

   ## 项目简介
   本项目是基于道义现实主义理论框架的LLM驱动多智能体社会模拟仿真系统，用于研究国际关系中国家的互动模式、战略决策和国际秩序演变规律。

   ## 技术栈
   - **后端**: Python 3.9+, FastAPI, SQLite
   - **前端**: React 18, TypeScript, Vite, Tailwind CSS
   - **LLM**: SiliconFlow (DeepSeek-V3.2)
   - **架构**: 前后端分离，RESTful API + WebSocket

   ## 快速开始
   ### 环境要求
   - Python 3.9+
   - Node.js 16+

   ### 安装依赖
   ```bash
   # 安装后端依赖
   pip install -r requirements.txt

   # 安装前端依赖
   cd frontend
   npm install
   ```

   ### 配置
   创建 `.env` 文件并配置SiliconFlow API密钥。

   ### 运行
   ```bash
   # 启动后端
   python -m backend.main

   # 启动前端（另开终端）
   cd frontend
   npm run dev
   ```

   ## 系统架构
   详见技术方案.md文档。

   ## 开发指南
   见docs/development.md。

   ## API文档
   启动后访问 http://localhost:8000/docs

   ## 许可证
   MIT License
   ```

2. **docs/getting-started.md** - 快速开始指南
3. **docs/user-guide.md** - 用户指南
4. **docs/api-reference.md** - API参考文档
5. **docs/configuration.md** - 配置参考
6. **docs/troubleshooting.md** - 故障排除

---

#### 任务3.2：创建部署配置

**文件位置**:

1. **Dockerfile**
   ```dockerfile
   # Dockerfile
   FROM python:3.9-slim

   WORKDIR /app

   # 安装依赖
   COPY requirements.txt .
   RUN pip install --no-cache-dir -r requirements.txt

   # 复制代码
   COPY . .

   # 安装前端依赖并构建
   WORKDIR /app/frontend
   RUN npm install && npm run build

   WORKDIR /app

   # 暴露端口
   EXPOSE 8000

   # 启动应用
   CMD ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8000"]
   ```

2. **docker-compose.yml**
   ```yaml
   version: '3.8'

   services:
     backend:
       build: .
       ports:
         - "8000:8000"
       environment:
         - DATABASE_URL=sqlite:///data/database.db
       volumes:
         - ./data:/app/data

     frontend:
       build:
         context: ./frontend
       ports:
         - "3000:80"
       depends_on:
         - backend
   ```

3. **scripts/deploy.sh**
   ```bash
   #!/bin/bash
   # 部署脚本

   set -e

   echo "开始部署..."

   # 构建Docker镜像
   docker-compose build

   # 启动服务
   docker-compose up -d

   echo "部署完成！"
   echo "后端: http://localhost:8000"
   echo "前端: http://localhost:3000"
   ```

---

#### 任务3.3：创建配置示例

**创建文件**: `config/`

1. **production.yaml** - 生产环境配置
2. **development.yaml** - 开发环境配置
3. **test.yaml** - 测试环境配置

---

### 阶段三Git提交流程

```bash
git add README.md
git add docs/
git add Dockerfile
git add docker-compose.yml
git add scripts/deploy.sh
git add config/production.yaml
git add config/development.yaml
git add config/test.yaml

git commit -m "docs: 完善项目文档和部署配置

- 添加README和项目文档
- 创建Docker配置和部署脚本
- 添加环境配置示例"

git push origin feature/framework
```

---

## 📋 智能体G任务 - 最终验收与发布

### 工作目录
```
C:\Users\yangy\myfile\python\ABM\v0.4.0
```

**说明**：智能体G在主工作区工作，负责集成所有功能分支和验收。不使用独立工作树。

### 最终验收任务（第9周执行）

```markdown
# 阶段三最终验收清单

## 1. 功能完整性检查

### 核心功能
- [ ] 所有智能体决策功能正常
- [ ] 互动规则完整实现
- [ ] 环境仿真准确运行
- [ ] 观测迭代层功能完整
- [ ] 工作流编排正常

### API功能
- [ ] 所有API端点正常工作
- [ ] WebSocket实时通信稳定
- [ ] 错误处理完善
- [ ] 安全措施有效

### 前端功能
- [ ] 所有页面正常显示
- [ ] 交互流畅无阻塞
- [ ] 数据可视化准确
- [ ] 状态管理正确

## 2. 性能验证

### 性能指标
- [ ] 响应时间符合要求（<2秒）
- [ ] 并发能力达标（支持10+并发）
- [ ] 内存使用稳定（<2GB）
- [ ] CPU使用合理（<80%）

### 压力测试
- [ ] 长时间运行稳定
- [ ] 大数据量处理正常
- [ ] 高并发场景稳定

## 3. 安全检查

### 安全措施
- [ ] API认证正常工作
- [ ] 输入验证有效
- [ ] CORS配置正确
- [ ] 敏感信息保护

### 漏洞扫描
- [ ] 无高危漏洞
- [ ] 依赖包安全
- [ ] 代码安全审查

## 4. 文档检查

### 文档完整性
- [ ] README完整准确
- [ ] API文档齐全
- [ ] 用户指南清晰
- [ ] 开发文档完整

## 5. 部署检查

### 部署配置
- [ ] Docker配置正确
- [ ] 环境变量配置完整
- [ ] 部署脚本可用

### 部署测试
- [ ] Docker镜像构建成功
- [ ] 服务部署正常
- [ ] 生产环境配置正确

## 6. Git合并流程

### 合并前检查
- [ ] 所有功能分支完成
- [ ] 代码审查通过
- [ ] 测试全部通过
- [ ] 无未提交的更改

### 合并到main
```bash
# 1. 切换到main分支
git checkout main

# 2. 拉取最新代码
git pull origin main

# 3. 合并各功能分支
git merge feature/framework --no-ff -m "Merge feature/framework: 完成框架和配置开发"
git merge feature/engines --no-ff -m "Merge feature/engines: 完成底层引擎开发"
git merge feature/entities --no-ff -m "Merge feature/entities: 完成核心要素层开发"
git merge feature/observation --no-ff -m "Merge feature/observation: 完成观测迭代层开发"
git merge feature/backend-api --no-ff -m "Merge feature/backend-api: 完成后端API开发"
git merge feature/frontend --no-ff -m "Merge feature/frontend: 完成前端应用开发"

# 4. 创建版本标签
git tag -a v1.7.0 -m "Release v1.7.0"

# 5. 推送到远程仓库
git push origin main
git push origin v1.7.0

# 6. 删除已合并的分支
git branch -d feature/framework
git branch -d feature/engines
git branch -d feature/entities
git branch -d feature/observation
git branch -d feature/backend-api
git branch -d feature/frontend

git push origin --delete feature/framework
git push origin --delete feature/engines
git push origin --delete feature/entities
git push origin --delete feature/observation
git push origin --delete feature/backend-api
git push origin --delete feature/frontend
```

## 7. 最终验收结果

### 验收状态
□ 通过
□ 不通过

### 验收问题
1. [问题描述] - [影响范围] - [优先级]
2. [问题描述] - [影响范围] - [优先级]
...

### 验收建议
[建议内容]

### 发布准备
如果验收通过，执行以下发布步骤：

1. 在GitHub上创建Release
   - 版本号: v1.7.0
   - Release notes: [填写发布说明]

2. 通知用户
   - 发布公告
   - 更新使用文档

3. 维护准备
   - 创建Issue追踪模板
   - 设置CI/CD流水线（可选）
```

---

*本文档按照第一阶段标准细化了第二、三阶段任务分配*
*创建日期: 2026-03-13*